CREATE TABLE messages (user_id TEXT NOT NULL, username TEXT NOT NULL, discriminator TEXT NOT NULL, message_id TEXT PRIMARY KEY NOT NULL, guild_id TEXT NOT NULL, channel_id TEXT NOT NULL, youtube_key TEXT NOT NULL);